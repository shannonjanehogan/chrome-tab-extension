// // var iconURL = chrome.extension.getURL("/icon.png");

// document.addEventListener("DOMContentLoaded", function(event) {

//     document.getElementById("iconURL").innerHTML = '<img src="/icon.png"/>';

// })

//use an iframe that points to the photo url
// <iframe src=""/>
// hardcode the id you are going to demo
// make sure that you don't want